# Library / Reader / Task 独立复审

2026-09-09。以本轮七张拒绝图及当前 course2md-design / system 为依据，先看完整画面，再检查几何和源码状态。只记录下列证据覆盖的范围，不作全产品验收结论。20–33 属于中间旧构建，不能替代本记录中的新图。

## 当前原生画面的整体判断

| 证据 | 静态判断 | 不能据此推断 |
| --- | --- | --- |
| 34-current-library.png | 有内容的库页按标题、检索与组织工具、笔记行展开。封面和名称为内容主线，分类与阅读操作在同一行；顶部导航的强调低于内容。输入、筛选及视图选择的实际高度比拒绝图一致。 | 真正空库、搜索无结果、分类恢复、刷新和打开笔记过程。单条合成笔记不能证明多条长标题的密度。 |
| 35-current-reader.png | 标题及来源/版本元信息与正文有区分；正文采用较窄阅读宽度。媒体已是可辨的正常矩形，未再压成 40px 横条；本图没有文件缺失警告。 | 目录、查找、版本更换、导出、正文长滚动与放大文字。初始正文约从画面 y=320 开始，元信息与操作头占比仍较高，需窄窗比较其影响。 |
| 36-current-reader-images.png | 截图卡形成图片、时间、转录、正文/原视频动作的阅读顺序，图像比例正常。单张内容保留自然空白，没有为了填窗补空卡。 | 多截图重排、不同图片比例和很长说明的实际截取边界。 |
| 37-current-image-viewer.png | 弹窗以完整图片为中心，上一张/下一张、缩放、适合窗口和关闭均可辨，文字说明与回到正文动作置于图片之后；单张时前后按钮的不可用状态可见。 | 多图前后移动、连续缩放、滚动边缘可达、关闭后阅读位置和焦点恢复。 |
| 56-narrow-tasks-200.png | 完成任务中，状态、产物去向、生成新版和阅读有清楚主次；阅读是当前强动作。标题与日期纵向偏紧，源码仍继承普通控件固定高度。 | 长标题、收起、运行/失败/部分完成和技术详情。 |
| 61-narrow-library-200.png | 搜索与组织工具已换成两行，但刷新按钮单独落为第三行，打断工具组与内容的连续关系。 | 后续修正后的原生几何。 |

蓝色封面与大图来自测试视频，不作为产品配色缺陷。以上截图也不证明选择器悬停、按下、焦点、切换动画或响应速度。

## 本次发现及处理状态

1. **部分完成的补做目标错误（源码确认）。** 原 `task_component_outcomes` 返回成功、未请求和失败全部结果，工作台注意卡将其全部列为未完成，最近笔记提醒还会取第一项作为补做目标。经整合者授权，已统一为 `task_component_failures`，只返回 Failed / Partial；队列、工作台注意卡、最近提醒及已生成结果卡共同使用。新增混合结果测试，覆盖成功截图、部分校对、未请求摘要、混合导出，以及全部成功/缺少 outcomes 的情况。测试由整合者运行；尚无当前原生部分完成截图。
2. **打开笔记提示重复（源码确认）。** views 的全局 reading 提示与库页/Reader 的局部提示同时触发，最近笔记也有第三种局部入口。已保留 Library、Reader 各自附近的一处；New、Task 及其他页保留壳层一处，并删除最近列表的重复。尚未捕捉修改后真实加载过程。
3. **任务标题继承固定高度（56 与源码）。** 两行标题/日期容器仍在 `control()` 的固定高度内，再加自身 padding。已改自然高度并清除最小高度；下一构建应检查长标题、200% 与展开/收起，不以解析成功作为关闭证据。
4. **库页刷新成为孤立第三行（61）。** 已移至库标题右端，表达对整个库的刷新；标题与刷新采用可分配空间，筛选/视图保持共同工具组。标题与工具栏复用原显隐条件，真正空库不因此多出管理工具。等待相同窄窗 200% 和普通字号画面。
5. **共享顶栏首项截字（56、61，未闭合）。** “工作台”显示成“工…”，其余短标签完整。已交整合者处理，不能用 34 的普通字号顶栏外观覆盖该问题。

## 源码状态审查与边界

- Library 区分正在检查保存位置、正在扫描、真正空库、空文件夹、搜索无结果、部分位置未连接及分类/名称恢复。搜索失败保留关键词和清除路径；不可访问不伪装成空库。这里确认的是分支与恢复入口存在，其长文案、滚动和操作反馈仍需真实失败画面。
- Reader 区分打开笔记、读取截图、无截图、文件不可读、部分处理未完成、旧版本打开失败、原视频位置失效与导出失败。正文保留与恢复操作在源码中分别存在。35–37 只覆盖文件齐全的单版本、单图内容，不覆盖这些错误分支。
- Task 的运行、暂停、取消、继续、未知远端结果、部分完成及完成都有独立条件。未知结果另行说明可能重复费用，并使用明确的重新发送动作；技术日志在展开区。56 仅确认完成页的首屏，不是全部状态的原生证明。
- 针对 `control()` 的多行/媒体调用进行了补查：库列表、卡片标题/封面、最近笔记、视频候选、文件夹长标签、Reader 图片和目录内容已显式使用自然高度。除任务标题外，本次没有再找到同一固定高度继承缺陷；不据此推断所有尺寸和组件状态已通过。

本记录中的定点代码修改仅以 `rustfmt --emit stdout` 完成语法解析；没有运行 Cargo、控制 GUI 或操作 Git。最后一次构建、测试、原生重拍以及未覆盖状态由整合者补齐。下方可继续追加后续证据，避免把“已改源码”写成“已验收”。

## 追加：0382e6f、860 窗口、200% 的原生复核

逐张查看 70–74，并读取 72、73 的 AX 文本。构建来源与固定 860 窗口由整合者提供；这组是同源 debug 构建。62、64 属于默认 1140 窗口的优化版，未混入这里的窄窗比较。

| 证据 | 本次实际所见 |
| --- | --- |
| 70-narrow-library-200-fixed.png | 顶栏“导入 / 笔记 / 任务 / 设置”均完整，首项不再成为“工…”。刷新位于“我的笔记”标题右端，搜索下一行，筛选和视图三项共同一行；61 的孤立刷新行消失，内容区前移。该 860 / 200% 静态条件下，前文第 4、5 项得到原生确认。 |
| 71-narrow-card-actions-200.png | 滚动到卡片下部后，内容数量、文件夹及更多操作位于阅读按钮之前。元信息没有再掉到主动作后面，文件夹与更多操作作用于同一张卡。阅读按钮下沿处于窗口之外，当前滚动位置不能证明整个卡片一次可见，也不构成被布局裁掉的证据。 |
| 72-narrow-note-menu-200.png | 笔记菜单在窗口内完整展开，重命名、打开位置及三个导出入口按作用分组；悬停项可辨。AX 确认导出项末尾省略号属于动作标签，并非文字被错误截断。此图只覆盖该次菜单展开/悬停，不能证明各导出动作完成。 |
| 73-narrow-rename-200.png | 对话框按名称字段、作用说明、取消/保存顺序排列；输入有足够高度，取消与保存同高且完整在窗内，保存为唯一强动作。标题、字段、说明和操作关系清楚，没有远端零散链接或空白大卡片。未见此状态的新排版缺陷。 |
| 74-narrow-task-natural-height-200.png | 完成任务标题和日期使用自然内容高度；两行之间及与分隔线之间保留空间，状态与阅读动作清楚。该短标题的 200% 完成状态未见挤压或重叠；仍不能代替多行长标题、收起及运行/失败/部分完成的画面。 |

整合者报告已实际打开菜单、进入重命名并点击取消；本审查直接检查的是对应静态截图和 AX，未自行操作。上述五张未发现需要再改代码的具体问题，但没有补齐 Reader 窄窗、加载、失败、导出结果或连续交互过程。前文对这些状态的边界继续有效。

另从 `tests-final.log` 核实新增混合 outcomes 测试已通过；这是补做范围逻辑的工程证据，不扩大以上静态设计结论。
