# UX 重构实施与实机验收记录

本页记录当时构建的功能验证。后续用户否定了改版后的设计与性能，不能将这里的功能结论扩展为当前界面设计通过；最新纠正见[设置页失败复盘](DESIGN-REVIEW-FAILURE-2026-09-08.md)，单输入取代草稿的变更见[回归修复记录](UX-REGRESSION-2026-09-08.md)。下文旧流程与截图保留其历史时间语境。

本记录对应 [重构规格](UX-REDESIGN-SPEC-2026-09-07.md) 和 [48 项审查](UX-AUDIT-2026-09-06.md)。本轮核心实现与回归已完成；最终结果见 [完整验收覆盖表](UX-ACCEPTANCE-MATRIX-2026-09-08.md)。用户取消的可访问性专项及外部环境限制单独列明，不把取消项计为通过。

## 验证环境与证据边界

- 实际设备：Apple M3 Max，macOS 27；真实 GPUI 原生桌面窗口，使用系统文件选择器和应用快捷键操作。
- 使用单独的验证应用包、配置与课程库：`/tmp/course2md-ux-validation`。不替换已安装的正式应用，不修改用户已有课程或服务配置。
- 视频、字幕和服务返回均为合成测试资料。桌面应用、工作进程、字幕解析、ffmpeg、内部发布及导出运行真实实现。
- 本地故障服务监听 `127.0.0.1:8769`，可分别返回有效正文、错误格式、401、429、接收后断开连接及延迟响应。请求日志仅保留模型、用途、字节数与媒体类型等验证字段。
- 在线分集、字幕和迟到响应由隔离应用的 `BV1UX*` 受控提取器提供，其余 URL 仍由安装的 yt-dlp 读取。受控依赖验证不能作为真实 Bilibili 平台连通性证据。
- 截图与对应原生可访问性树保存在测试目录的 `screenshots` 中。AX 树检查和实际 VoiceOver 操作分别记录，互不替代。
- 自动测试用于核验持久化、版本保护和网络边界，不代替页面上的实机观察。

## 已完成的实机观察

以下证据来自第一轮验证构建。后续修复的页面需要再次验证，不能以旧截图代替最终效果。

| 场景 | 实际结果 | 证据 |
| --- | --- | --- |
| 首次使用与字幕生成 | 空库可直接进入生成；选择带同名字幕的本地 A，不配置 ASR、关闭 AI 与全部附加导出，真实生成并在应用内阅读正文 | `01-library-empty`、`02-generation-plan`、`03-task-complete`、`04-internal-note-body` |
| 主动测试 AI 服务 | 实际发送用途契约请求，校验返回正文后显示通过；保存后才成为有效的默认服务版本 | 服务请求日志、独立服务配置记录 |
| HTTP 200 错误格式 | 显示缺少本用途所需有效正文，没有仅凭 HTTP 200 宣告成功 | `05-test-invalid-200` |
| 401 与限流 | 分别说明凭据/权限问题与额度/速率限制；没有捏造重试倒计时 | `06-test-401`、`07-test-429` |
| 测试结果不确定 | 接收后断开连接，说明可能已产生费用，未自动重测 | `08-test-unknown` |
| 有效服务与未保存草稿隔离 | 服务草稿把模型改为 `ux-next-unpublished` 后未保存；随后 B 的计划和实际请求仍使用已保存的 `ux-fixture` 版本 1 | 服务配置记录、B 任务快照、请求日志 |
| 真实任务的未知云请求 | B 的一次校对请求被服务接收后断开；已生成的正文保留可读，请求未自动重发 | `09-unknown-task`、`10-partial-unknown`、请求日志 |
| 明确重发与独立草稿 | 用户动作明确重发后才产生第二次课程校对请求；后台完成期间可选择另一份无字幕视频 C，来源草稿保持为 C，没有跳回 B | `11-independent-c-draft`、任务/草稿记录、请求日志 |

第一轮发现并进入修复的界面问题：长表单的主按钮离开视口、已确认视频仍重复显示大块拖放区、错误主体重复显示中英文内部报错、任务阶段出现内部标识、阅读正文与状态的原生语义不完整、阅读位置与截图操作不足、弹层背景仍进入可访问性范围。完成对应修复后重新验收。

### 第二至第四轮的真实观察

| 场景 | 实际结果与边界 | 证据 |
| --- | --- | --- |
| 历史文稿发现 | Markdown、HTML、JSON 三种可读历史资料能进入课程库；没有正文的目录留在历史任务材料。10 份原文件的哈希没有变化。早期 `/tmp` 与 `/private/tmp` 导致侧栏少计，已修复，第四轮侧栏实际计数与库一致 | 历史资料哈希记录、原生课程库 |
| 阅读与弹层 | 历史 HTML 的段落、列表和说明可读；截图对话框有真实说明和相邻文字，背景不进入 AX 树。查找“手工”报告 3 处命中。实际 VoiceOver 全流程仍未通过 | `12-v2-image-modal`、AX 记录 |
| 两种复制 | 粘贴到真实 TextEdit 文稿验证了完整纯文本与 Markdown 文本；没有把图片文件伪称为剪贴板内容。发现旧复制按钮同时弹出菜单并复制，已拆成两个明确动作 | `13-clipboard-plain`、`14-clipboard-markdown` |
| 网页独立导出 | 原生保存面板产生 6,066 字节 HTML，内含图片；暂时移走原课程库后在 Chrome 打开，正文末段和图片仍可用。Safari 被该设备的文件访问限制阻止，本项不记 Safari 通过 | `16-portable-html-chrome`；`15` 为 Safari 未通过记录 |
| 课程库离线 | 不会重新创建空库。实机发现“其余笔记仍可打开”“没有匹配的课程”误用于完全离线，已修改状态聚合，仍需用最终构建复测 | `17-library-offline` |
| 最小窗口首次绘制 | 860×620、200% 字号下，第三、四轮停留首帧；缩放窗口后才呈现已发生的导航。采样无死锁，诊断钩子未捕获 panic。已增加原生重绘唤醒与初始焦点同步，待第五轮验证 | 原生进程采样、`19-v4-initial-render-fault` |
| 分集与法语字幕 | 合辑先要求单选；选 `p=2` 后显示第二节及处理 1 个视频。手动选择法语，实际任务来源身份含第二节，正文为法语，未退回中文 | `21-p2-french-plan`、`23-p2-french-submission`、`25-french-readable-body` |
| 新建文件夹 | 生成页弹层创建指定文件夹后，草稿和计划都采用新归属；弹层期间生成快捷键没有改变背景，AX 背景范围隐藏。原生字段焦点尚未通过，不能由此推定完整键盘/VoiceOver 通过 | `22-folder-modal-navigation-blocked` |
| 摘要独立开关与部分成果 | 关闭校对、开启摘要、仅选网页；摘要服务拒绝请求后，正文、图片、网页仍保存。请求没有音频或图像，内部阅读显示所选法语正文 | `24-french-body-summary-failed`、`25-french-readable-body`、服务日志 |
| 局部补做的实机缺陷 | 第四轮点击“仅补摘要”没有新请求，却发布了另一部分完成版。真实组合测试定位到旧的确定失败请求被当作禁止重发；已修复显式补做授权，并收紧错误格式降级，等待原生复测，不能记本场景通过 | 任务 `task-18d2d2dad898cfa8-ebc3-1`、服务日志 |
| 普通偏好写盘失败与重试 | 独立偏好目录暂时只读，减少动态效果无法保存；恢复权限后点击重试，原选择被保存并生效。有效服务仍为 `ux-fixture` 版本 1，未保存的 `ux-next-unpublished` 草稿仍在 | `26-preference-write-failed`、`27-preference-retry-succeeded`、`28`、`29` |
| 提交前同步设置 | 默认 JSON 更改保存失败时立即返回生成并提交：任务数、服务请求数、视频下载调用数均未增加。恢复保存后，新草稿继承 JSON，已有仅网页覆盖草稿保持原选项。发现缺就地恢复入口及残留错误，修复待复测 | `30-generation-blocked-by-unsaved-default`、前后计数、草稿记录 |
| JSON 与后台完成 | 仅选 JSON 的新任务成功，在内部阅读页仍有正文和图片；停留设置期间任务完成，没有强制跳页。尚未由本次操作验证“运行期间修改默认”，因为更改动作发生前任务已结束 | `31-background-success-stays-in-settings`、`32-json-only-internal-reader` |

第四轮原生发现后进入修复、尚不能仅据编译宣布关闭的问题还包括：自动标题误标为手工名称、中文字幕语言匹配次序、长文件夹名称竖排、保存失败的重复原始异常、处理失败错误地提供“重新读取文件”、互斥控件使用切换按钮语义，以及输入框的原生焦点仍停在窗口。第五轮之后须逐项记载复测结果。

### 第五轮的真实观察（构建 fd35a8b 及 b4b2715，标签 v5 / v5m / v5m2 / v5m3）

| 复测项 | 实际结果与边界 | 证据 |
| --- | --- | --- |
| 自动标题误标为手工名称 | 通过。新草稿读取 BV1UXAAA1 后名称自动填入；改链接到 BV1UXBBB2 名称跟随更新；手工改为“我的手工笔记名”后换 BV1UXCCC3 名称保留 | 原生 AX 字段值序列 |
| 中文字幕语言匹配次序 | 通过。合成来源（原声日语，fr/ja/zh-Hans/ai-zh）默认选中简体中文人工字幕而非 ai-zh；“更换文字来源”列出全部语言与“中文 · 自动字幕”标记，含“识别视频声音” | AXRadioGroup（笔记的文字来源） |
| 长文件夹名称竖排 | 通过但有新发现。侧栏 24 字文件夹名两行横排截断、完整名在悬浮提示与 AX 标签。新发现列表模式笔记卡片的文件夹选择在 200% 字号竖排，已在 b4b2715 修复（rems 宽度＋两行截断）后复测通过 | `v5m-200-first`、`v5m2-first`、`v5m3-first` 截屏 |
| 保存失败的重复原始异常 | 通过。只读偏好目录下切换“减少动态效果”：首层仅“应用偏好未保存：设置目录暂时无法写入”，开关回到实际生效值，侧栏“设置，未保存”，原始 os error 只在“查看技术详情”；恢复权限点“重试保存”后生效并清除 | 原生页面与重试后状态 |
| 处理失败提供“重新读取文件” | 通过。摘要被服务拒绝（HTTP 400）的任务与阅读页均只有继续/补做/阅读/技术详情类动作，没有“重新读取文件”；正文与截图可读 | 任务页、阅读页 AX 与截屏 |
| 互斥控件使用切换按钮语义 | 通过。视频来源、文字来源、识别方式、识别模型、设置页签、服务协议/认证、文字大小均为原生 RadioGroup/RadioButton 或 TabGroup | 各页 AX 树 |
| 输入框原生焦点停在窗口 | 通过。VoiceOver 开启后焦点落在“笔记名称”文本字段，字幕面板报“You are currently on a text field”。System Events 读不到 AXFocusedUIElement 是该查询路径的局限，不能作为失败证据 | `vo-screen` 全屏截屏 |
| 局部补做（第四轮缺陷） | 通过。服务恢复有效后点击“仅补摘要”：服务日志恰好新增 1 次摘要请求，提取器请求数不变（未重下视频/重做字幕截图），任务转为“笔记已生成”，补做任务可追溯，笔记发布版本 2 且摘要置于开头、正文保留、可选旧版 | 服务/提取器日志前后计数、任务页、阅读页 |
| 最小窗口首帧 | 通过。860×620 与 200% 字号下启动即完整渲染课程库，无需缩放窗口 | `v5m-first`、`v5m-200-first`、`v5m3-first` |
| 课程库离线聚合 | 通过。移走库目录后未重建空库；刷新后显示“保存位置暂时无法访问”与“重新检查保存位置”，侧栏“暂时无法读取”，不冒充首次空库；恢复目录后重新检查即回到笔记列表 | 原生页面两阶段截屏、目录检查 |
| 提交前同步设置（就地恢复入口） | 通过。生成页出现“重试保存生成选项”就地入口；未保存期间点“生成新版笔记”被阻断，任务数、服务请求数、提取器请求数均不变；恢复权限重试后 JSON 导出偏好落盘、错误在设置与生成页同时清除 | 生成页两阶段截屏、前后计数、generation.json |
| 200% 主导航裁切 | 新发现并已修复。“生成笔记”标签在 200% 被裁为“生成笔讠”，b4b2715 改为最小高度＋换行后复测完整 | `v5m-200-first`、`v5m3-first` |

VoiceOver 本轮只做了焦点抽查；完整 VoiceOver 生成/修复/继续/阅读流程（AT33）仍未执行，不得由焦点抽查推定通过。

模拟故障后的课程库目录与设置目录权限均已恢复。测试服务和媒体工具的模式文件是独立验收条件；切换模式不修改正式产品配置。

### 第六轮的真实观察（构建 17d49d1 / 9f5dda3 / fe8da4b，标签 v6 / v6m）

第五轮的 200% 修复经用户实机复查仍判定不可用（列表行文件夹列竖排挤压、整体几何失调）。本轮改变方法：先由 [Web 高保真 mock](ux-mock/)（`?view&scale` 参数覆盖 100%/200%/窄窗）校准几何规则，再复刻到 GPUI，最后实机逐场景对照。结构尺寸全部 rem 化（侧栏 208→416px@200%、封面 96×54→192×108@200%），发丝线/padding/图标保持 px。

| 场景 | 实际结果与边界 | 证据 |
| --- | --- | --- |
| 列表 100%（1140 窗） | 通过。行不再换行：封面、标题、meta 完整；chip 单行「2026 年秋季学期金融风… ⌄」；⋯ 菜单五项齐全且实际展开；chip 菜单含「未分类」与当前文件夹勾选 | `v6-list-1x`、原生菜单交互截屏 |
| 列表 200%（1140 窗） | 通过。侧栏 416px、导航完整；标题两行换行；chip「20…」、meta「1 段笔记 · 1 张…」均有省略号；无竖排、无字形撕裂 | `v6-2x` |
| 列表 200% 窄窗（860×620） | 通过。内容区 <24rem 触发降级：chip 只留 📁▾、封面降档 48×27、行距 12→8px；标题两行「我的/手工…」；工具行换行不溢出 | `v6m-2x` |
| 卡片 100%（1140 窗） | 通过。封面 16:9，底栏 chip 单行省略 + ⋯ 同行 | `v6-cards-1x` |
| 按文件夹分组（100%/200%/窄窗，减少动态效果开启） | 通过。分组头单行省略号；展开/收起即时；组内行渲染与平铺一致 | `v6-grouped-1x`、`v6-grouped-2x`、`v6m-grouped-2x` |

复刻过程实机曝光并修复了四个存量缺陷（均为本轮之前的代码引入，此前从未被实机触及；复测证据即上表）：

1. **截断无省略号且撕裂字形**：本仓库 GPUI 的 `line_clamp` 只限制行数，末行越界硬裁。全库截断点改为 `text_ellipsis` 配方（复刻注意事项见 ux-mock README）。
2. **卡片底栏 ⋯ 被挤出**：gpui-component Button 的 `flex_1` 在含长文本 child 时实测失效；chip 改为布局方显式计算 `max_w`。
3. **库根未注册导致分组被静默禁用（17d49d1）**：有工作区记录时启动库根指向 `desktop-local-library`，与已注册库根不符，`library_error` 常设且无界面提示，「按文件夹」被禁用。现回退到工作区首个可读位置。
4. **减少动态效果下分组内容恒不可见（9f5dda3）**：MotionReveal 首帧零高、依赖后续帧测量，该路径自 eced30b 引入起在验证环境从未可达（被缺陷 3 的门控掩盖）。`disclosure` 在 reduce-motion 下直接渲染终态；非 reduce-motion 的展开/收起动画本轮只做了交互抽查。

边界：封面纯蓝块是合成夹具的缩略图数据，不是版式问题；200% 中等宽度下 chip 只剩「20…」属 mock 既定取舍（信息量少但不破版）；阅读页、生成页、设置页的 200% 几何未在本轮范围；缺陷 3 修复前分组视图在本环境从未渲染过，不能由本轮推定其历史行为。

### 第七轮的真实观察（2026-09-08，设置页 M7 与草稿/字幕复测）

当前界面已经采用 [app.html](ux-mock/app.html) 的顶部导航与居中内容列。第六轮的侧栏截图保留为历史证据，不能代表现在的页面结构。本轮接续工作区内未完成的设置页改动，完成四个设置页签、胶囊单选、服务卡片与就地编辑、存储和应用诊断；设置根据实际进入位置提供返回工作台、我的笔记、阅读或任务的入口。

实现提交：`2ff29fb`（胶囊单选与焦点滚动组件）、`510220e`（设置页 M7）、`650a963`（字幕错误摘要与技术详情）、`fdf0720`（当前帧完成后再判断焦点归属）。v19 含临时焦点几何日志，仅作中间证据；v20 对应 `510220e`，v23 对应 `650a963`，v24 是焦点修复的待提交构建，v25 / v25m 对应 `fdf0720`。临时几何日志未进入提交。

| 场景 | 实际结果与边界 | 证据 |
| --- | --- | --- |
| 设置页四个分组 | 最小窗口下生成默认项、服务与账号、存储、应用均可访问；服务卡片显示实际模型与停用状态，编辑时标明实际版本。模型缓存完整明确标为“尚未验证能否加载”；存储显示真实路径，不编造空间占用 | `v23-settings-entry`、`v23-settings-app-100`、`v25m-services-100`、`v25m-storage-100` |
| 设置页键盘与 200% 字号 | Tab 从当前页签进入选中的单选项，方向键与 Home/End 保持互斥选择。发现仅在渲染时判断焦点归属会偶发漏掉第一次滚动；改为当前帧完成后判断并滚动。v24 连续三轮 100%→200%→Tab，开关及说明均进入视口，覆盖“已保存”提示出现和消失。只验证设置中的这条路径，不推定全部控件或完整 AT34 通过 | `v23-app-radio-200`、`v23-app-switch-200-settled`（修复前失败）、`v24-switch-cycle-1`、`v24-switch-cycle-2`、`v24-switch-cycle-3` |
| 已提交构建的宽窗/最小窗口对照 | v25m 的 860×620、v25 的 1140×760 均在 100%→200% 后通过第一次 Tab 将开关和说明滚入视口；宽窗下生成设置与应用设置保持居中列。最终关于信息确认构建 `fdf07209a40a` | `v25m-switch-200`、`v25-general-wide-100`、`v25-app-wide-100`、`v25-app-wide-200` |
| 测试已保存服务时已有另一份编辑草稿 | 实机暴露旧实现可能测试到当前编辑器内的其他草稿。现在先保存并切换到目标服务，成功后才发起测试。将草稿模型改为 `ux-unpublished-v19` 而不发布，再点已保存服务的“测试服务”：日志恰好增加 1 条，模型为 `ux-fixture`，用途为校对；未发布模型草稿仍保留，默认服务及其版本 2 不变 | `v19-service-unpublished-model`、`v19-service-correct-test`、`v19-service-target-evidence.json`；请求数 11→12 |
| 从设置回到原位置 | 从阅读页进入显示“返回阅读”，返回后仍为原笔记版本与正文；从 B 草稿进入显示“返回工作台”，返回后 B 的链接及实际输入焦点恢复。未在本轮验证长文中段锚点，也未覆盖 AT09 的文件夹覆盖与仅本次服务保存全组合 | `v19-settings-from-reader`、`v19-reader-return`、`v19-settings-from-draft`、`v19-draft-return-fast-b` |
| A 读取期间替换为 B，在线与本地草稿重启恢复 | 受控 A 为 `BV1UXSLOW19`，B 为 `BV1UXFAST19`；等待超过 A 的 25 秒延迟后，B 的来源和中文字幕仍保留。系统文件选择器选择本地 C 后，在在线/本地之间切换可分别找回 B/C；退出并启动 v20 后仍分别恢复 B 的中文字幕与 C 的本机识别计划，JSON 选项保留。文件夹均为未分类，本轮未覆盖两份草稿的不同文件夹与选项覆盖组合 | `v19-online-b-before-restart`、`v19-local-c-before-restart`、`v19-source-drafts.json`、`v20-online-b-restored`、`v20-local-c-restored` |
| 无字幕、字幕超时、需要登录三态 | `BV1UXNONE20` 明确显示未找到字幕并提供识别方式；超时与登录受限均保持“尚未确认”，不自动选中语音识别。超时解释网络重试，登录受限另有字幕登录修复入口；原始提取器错误默认收进技术详情，展开后可读，重试只展示本次原因 | `v20-subtitles-none-explained`、`v23-timeout-visible`、`v23-login-retry`、`v23-login-details` |
| 字幕恢复后重新读取 | 在受控提取器中分别恢复同一个 LOGIN/TIMEOUT 来源，原生点击“重新读取字幕”后选中已读取的简体中文；来源、名称、未分类归属、JSON 与关闭的 AI 选项保留，失败提示消失。两份草稿均有实际解析后的字幕正文；任务总数仍为 14、服务请求总数仍为 12，没有启动生成或外发音频 | `v23-login-recovered`、`v23-timeout-recovered`、`v23-subtitle-recovery-evidence.json` |

字幕恢复使用新夹具文件 `subtitle-recovery.json`，只让列出的 `BV1UX*` 合成来源恢复可读；不模拟真实账号登录成功。本轮结束已移除该临时覆盖，减少动态效果保持原值，字号恢复 100%。AT04 的受控三态和重新读取路径已有证据，真实 Bilibili 登录及平台连通性不在该证据范围内。

自动验证：`cargo test --features integration` 共 165 项通过、2 项忽略；`cargo test --manifest-path desktop/Cargo.toml` 共 103 项通过、3 项忽略。桌面 debug 构建通过，`git diff --check` 通过。另验证了字幕夹具的默认失败、按来源恢复以及无字幕状态互不影响。编译仍有依赖的弃用/未来兼容提示及现有未使用代码警告。

## 验收追踪

第八轮起逐项覆盖和剩余条件集中记录在 [完整验收覆盖表](UX-ACCEPTANCE-MATRIX-2026-09-08.md)。用户要求完成全部核心流程，并于 2026-09-08 明确取消可访问性专项；AT33 及 AT34 的全页面纯键盘、200% 字号专项不再作为本次完成条件。历史观察保留，不把取消范围记为验收通过。

第八至十一轮已补齐核心组合回归并完成原子提交：草稿/服务/默认值隔离，三种导出组合的内部阅读，实际 AI 和 ASR 请求，下载与发布中断，独立版本锚点，分类和迁移恢复，以及旧配置升级。最后实机发现的缓存模型联网等待、后台结果无提示/任务入口缺失、退出抹去失败状态均已修复并复验。

最终验证：核心 169 通过、2 忽略；桌面 108 通过、3 忽略，原生 debug 构建通过。本机缓存 Qwen3 1.7B 对同一合成音频的完整识别由约 235 秒降至约 1.09 秒，0 Hub 请求；受控云端音频上传与发布成功。退出运行任务后冷启动保持暂停，失败/未知请求保留原状态与处理入口。

完整证据、提交和限制详见覆盖表第十/十一轮。自动恢复边界采用真实文件和进程的受控故障测试，没有重启宿主系统。独立 HTML 视觉打开沿用第四轮 Chrome 记录；本轮新导出的结构与图片已验证，但浏览器工具拒绝本地文件，未绕过该限制。历史各轮的“待复测”描述保留其时间语境，不代表最终状态。

## 重现入口

测试脚本：`desktop/scripts/ux_fixture.py` 和 `desktop/scripts/ux_ytdlp.py`。

```sh
python3 desktop/scripts/ux_fixture.py prepare
python3 desktop/scripts/ux_fixture.py serve
# 两个 debug 可执行文件构建完成后，在另一终端打包隔离应用：
python3 desktop/scripts/ux_fixture.py bundle --label v2
```

`prepare` 创建合成视频/字幕并将服务响应重置为有效结果。需要保留正在进行的故障场景时不要重复调用。`bundle` 只复制已构建的本机程序，不代替构建或测试。测试服务仅用于合成内容。

字幕恢复复现：先在隔离应用读取 `BV1UXLOGIN20` 或 `BV1UXTIMEOUT23`，观察失败后，在验证根目录创建 `subtitle-recovery.json`，内容为 `["BV1UXLOGIN20", "BV1UXTIMEOUT23"]`，再在原草稿点击“重新读取字幕”。仅列出的来源恢复，其他失败来源仍保持原状态；完成后移除这个文件。`prepare` 不会清除该覆盖文件，重跑故障场景前须自行恢复此项条件。
