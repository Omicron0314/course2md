**原生流程独立复核 · 2026-09-09 · 中间构建**

依据当前 course2md-design 的 system / interaction / settings 规则，以及用户原始工作台、笔记库批注，逐张查看了 16–23 的 PNG，并读取同编号 AX 文件。这里只记录这些画面支持的结论；没有操作 GUI、修改源码或运行 Cargo。该组以暗色、100% 字号及合成课程为主，不能代替整产品验收。

这组画面已经形成可辨认的输入、当前任务、生成结果、阅读器、设置返回和再次输入关系。工作台的主动作贴近视频输入；运行进度留在所属任务附近；19 确实显示阅读器正文区域，22 显示库列表。仍需收敛完成后的信息层级与任务说明，并补齐返回位置、异常恢复和实际图文阅读的证据。

| 页面 / 证据 | 实际可见状态及问题 | 不能由该画面推出的结论 |
| --- | --- | --- |
| 输入 · 16-source-entered-no-auto-start | 标题、来源选择、视频输入与“开始转换”构成一条明确的主任务；默认字幕策略和外发说明为次级文字，高级选项收起。没有原图中包住普通输入的额外大框，也没有“新建笔记”入口。该状态未见新的具体排版缺陷。 | 截图与 AX 可确认输入值，不能独立证明此前没有后台请求，也不能证明 Enter 与点击触发相同次数。 |
| 运行 · 17-single-start-progress | 当前任务卡与当前链接相邻，任务标题、进行中状态、暂停和取消可辨认。说明行显示 `2026-09-09 01:22 UTC 开始`，并把同一个服务地址写了两遍：`文字发送到「127.0.0.1」（127.0.0.1）`。这是可见的信息冗余；时区格式也要求用户额外换算。 | 不能证明暂停、取消已经生效，不能证明关窗与退出时的任务行为符合说明。AX 是“树未变化”记录，不能据此认定状态控件不存在。 |
| 完成 · 18-single-start-complete | 结果通知带“查看生成结果”；但受理通知仍在，下面又出现“准备生成”和重新生成相关内容。完成后的阅读顺序混杂，正文入口被继续生成的说明挤向下方。此项已交由 shell 修复；本图不代表修复后的状态。AX 同时列出打开已有笔记、生成新版、计划详情、最近笔记等后续操作。 | 18 只证明结果入口出现；它本身没有显示阅读器，也不能证明生成文件完整。 |
| 阅读器 · 19-result-opens-reader-directly | 可见同名笔记标题、版本、来源、笔记/截图切换、阅读工具和正文图片。上下文返回指向工作台；全局“我的笔记”处于当前状态。图片保持宽高比例，没有被普通控件高度压扁。 | 文件名不足以证明“只点击一次直接打开”；这一动作次数需主任务的运行轨迹补充。蓝色画面是合成素材，不应据此报图片加载失败。首屏没有正文段落，主要被元信息和首图占据；需用代表性的图文笔记继续评价阅读密度。 |
| 设置 · 20-reader-settings-return | 设置为当前位置，明确提供“返回笔记”。导航与内容分开，模式、字体和开关使用同一控件起始轴，主题预览保留独立浅/深配置。所示初始画面未见新的具体截断或层级缺陷。 | 没有观察到设置修改或失败，不能证明保存、主题切换、焦点、动画或任何未展示设置页已通过。 |
| 返回阅读 · 21-return-preserves-reading-position | 返回后仍为同名笔记、同一首图，正文已可见。没有落回库列表或空阅读器。 | 与 19 相比，图片在正文视口中约向上移动了 52px；附件没有“离开设置前最后一次滚动位置”的画面。因此只能确认文档恢复，不能确认滚动偏移逐值保留，也不能把这个差异直接判为回归。 |
| 库列表 · 22-active-library-tab-opens-library | 当前全局栏目仍是“我的笔记”，内容已切换为库列表；筛选、组织方式、列表/卡片同排，刷新在页标题处，唯一笔记行有版本日期、分类、阅读和更多操作。该短标题、单行列表状态未见新的具体布局缺陷。 | 不能覆盖空库、搜索无结果、读取失败、长标题、多条目、菜单、卡片视图及放大字号；AX 只有焦点记录，不能单独证明所点击的是此前已选中的导航项。 |
| 再次生成 · 23-second-source-enter-start | 输入和当前任务均显示 SECOND，FIRST 没有混入当前任务标题。阶段完成标记与当前截图进度区分清楚，暂停/取消仍可见。UTC 与重复服务地址的问题在第二次任务中仍存在，说明并非只有首次任务卡如此。 | 不能证明只发送一次生成请求、复用了正确的默认配置、FIRST 文件未被替换，也不能证明第二次任务已完成。需要操作日志、服务请求记录或产物核对。 |

**需要处理或补证的项目**

- 完成界面应先回答“结果在哪里”，结束受理阶段后移除旧受理提示；重新生成保留为后续选择。18 的修复须用新截图复核，不能用已改源码代替。
- 任务卡的服务显示名与 host 相同时只显示一次；名称不同才保留用于辨认服务的地址。17、23 提供了相同地址重复的直接证据。
- 面向用户的开始时间应使用本地时间或明确的相对时间；精确 UTC 可以留在详情。时间内容有用，不需要在普通任务卡里要求时区换算。
- 补拍离开设置前与返回后的同一阅读位置，或记录连续操作与对应 offset。现有 19/20/21 只能证明返回同一文档。
- 补一篇包含真实讲义画面、标题和长短段落的代表性笔记。合成蓝图验证了图片可显示及比例，但不能验证图文的阅读节奏。

本轮截图没有覆盖窄窗、125/150/200% 字号、浅色、来源读取中的状态、读取失败、生成失败/部分结果、真正空库、阅读器加载失败、按钮按压或动画过程。这些缺口是证据范围，不是已发现的缺陷。没有进行专项可访问性审计。

**追加复核 · 29–35 后续原生构建**

逐张查看了 29–35 PNG，并读取同名 AX 文件。29 的 AX 提供完整控件树；30–35 仅记录“树未变化”，这些页面以 PNG 为证据。本节没有操作应用。补充源码阅读仅用于确认模型运行时返回动作的禁用条件，不把源码存在当成真实点击验证。

从整页关系看，完成页已经围绕可阅读结果安排主动作，引导的标题、表单和底部动作共享较窄内容边缘。相关文字不再被分散到整窗两端。以下是逐页实际范围，不代表所有状态或全产品设计验收。

| 页面 / 证据 | 实际可见结论与具体问题 | 证据边界 |
| --- | --- | --- |
| 完成 · 29-completion-new-build | 当前 SLOWBACKGROUND 的“笔记已生成”、课程名、“已保存到课程库”和主按钮“阅读笔记”形成明确顺序；“转换下一个视频”次级，“调整并重新生成”更弱。18 中当前结果下面重新出现“准备生成”的混杂已经消失。顶部还保留 SECOND 的另一条完成通知，标题明确区分目标，不能当成当前任务的两份重复结果。 | 静图不能确定另一条通知何时发生、是否已读，也不能证明两个入口各自打开正确笔记；需结合主任务操作记录。 |
| 连续设置往返 · 30-reader-position-before-settings / 31-reader-position-after-settings | 两张为同一课程和版本，首图上缘、图注与正文首行处于相同可见位置；本次往返没有可见跳动，也没有落回库列表。它们补足了 19/21 缺少离开前最终画面的限制。 | 只覆盖这篇合成课程、这次返回与当前位置；不能推出任意长文、版本切换或重启后均保留精确偏移。蓝色合成图片仍不能评价真实讲义的图文密度。 |
| 重入引导 · 32-guide-restart-preserves-engine | 显示“设置默认识别方式”，明确用于后续生成；Automatic 与 Qwen3 0.6B 为当前值，识别方式与模型在同一控件轴。**仍有确定文案矛盾：检测说明写“模型在下一步准备”，实际第二步是 AI 服务；下方又写“模型在最后一步准备”。**建议检测说明仅保留“运行环境可用”，由下方说明准备时机。对应 `desktop/src/onboarding.rs` 的 `setup-detected-engine`。 | 显示选中值不等于证明全部既有配置逐字节保留；需要原设置或持久化比对。底部固定动作的大片间隔服务于引导导航，本图未见控件脱离所属表单或异常空卡。 |
| 既有 AI 配置 · 33-guide-existing-ai-compact | 地址、模型 ID、认证轨道与两个能力开关共享起始轴；标签和输入位于共同窄表单中，较早宽表单的割裂不再出现。正文只提供次级“检查当前配置”，底部为“保存并继续”，没有同名重试动作重复。可略过 AI，也可返回。 | 此图为“无需认证”和既有配置状态，没有 API Key 输入、显示密钥动作、字段错误或 HTTP 失败。因此不能由 33 宣称这些新布局和恢复状态已在原生画面通过，也不能证明保存时是否实际复测或发送请求。 |
| 模型未完整 · 34-guide-model-resume | 识别模型、未下载完整状态、复用已下载文件说明与准备动作集中在一起；状态中性，技术内容折叠。准备按钮为主动作，底部“稍后准备”次级，没有并列两个强主动作，也没有声称模型已可用。 | “复用文件”是界面说明；真正断点复用、文件大小和是否重新下载需运行记录证明。本图是重入后的部分缓存状态，不是新一轮点击暂停后的终态。 |
| 模型进行中 · 35-model-resumed-download | 显示“正在准备模型”、暂停准备和可后台继续的出口。尚无实际进度样本时只显示运行状态，没有旧图中的空下载 0 MB 行或由 Apple 阶段比例推算出的 ETA。 | 单帧不能证明文件正写入、暂停有效、真实样本到达后的进度布局或后台条高度。左侧“返回引擎选择”在源码由 `model_running` 禁用；截图不能替代禁止回改引擎的实际点击验证。 |

新增需修项只有 32 的步骤时机文案；本轮没有从这些画面发现新的明确几何缺陷。原 17/23 的普通 UTC 时间显示已另行用共享本地时间函数修正，但这些截图早于该修改，不能用来验证本地时区展示。测试、原生本地时间、Key 与错误表单、模型新暂停终态和真实课程图文仍由各自证据覆盖；不以工程测试数量推定视觉或交互通过。

**追加复核 · 50–61 合集、本地视频与课程库恢复**

逐张查看了 50–61 PNG 与同名 AX，未查看 59a 系统选择器排障图，未操作 GUI。50、52、53、54 有可读控件变化，56 有 QuickTime 完整窗口信息；其他 AX 多为未变化或焦点记录，不能据此推断控件缺失。该组是宽窗、暗色、100% 字号的隔离运行证据。

这组流程在需要用户判断的视频合集处停下，选择后出现所属任务及完成结果；本地文件也到达可阅读结果。课程库失联有原位置和重新定位入口，恢复后能回到原笔记。局部仍有三个信息或步骤问题：候选只有难区分的 URL、本地文件选中后文件名层级过弱，以及重新定位成功后仍需关闭阻断层。

| 页面 / 证据 | 实际可见结论与具体问题 | 证据边界 |
| --- | --- | --- |
| 合集选择 · 50-collection-selection | 合集名、选择说明和两个候选位于输入之后，选择目的可辨认。但两个候选仅呈现同一长 URL，差别在末尾 `p=1/2`；快速辨认章节仍依赖读链接。源码直接使用 `candidate.title`，当缺少题名而回退为 URL 时，可补“第 1/2 个视频”主标签并保留链接。另有一个候选暂时无法确认时，画面显示红色错误卡和“重试转换”，其强度压过仍可用的选择。 | 主任务已把可选候选之后的错误改为普通帮助文字；50 为修改前画面，不能据此宣称该状态已在新原生构建复核。候选标题缺失不能推断实际视频缺少题名，可能是当前元数据只返回链接。 |
| 选择后继续 · 51-collection-choice-continues | 当前输入为 `p=2`，任务标题为“验收课程 · 第 2 节”，运行状态与暂停、取消相邻。无需重新按另一处开始按钮的页面关系清晰。开始时间呈现为 `2026-09-09 09:55`，没有 UTC 后缀；同一个服务 host 也只出现一次。 | 连续操作是否只提交一次任务由操作和任务记录补证；静图只支持所选输入与进行中任务相符。无 UTC 后缀不单独证明跨时区日期换算，跨日期逻辑仍由语义测试覆盖。 |
| 合集完成 · 52-collection-result | 清楚显示第 2 节笔记已生成、已保存，主动作是阅读笔记；转换下一个视频和调整重生为次级。没有叠加同一任务的受理提示。该完成状态未见新的具体排版问题。 | 52 本身没有阅读器正文，产物完整性由 manifest 和任务结果补证。 |
| 本地输入 · 53-local-file-selected | 本地来源为当前选择，`lecture-a.mp4` 已列出，“更换视频”与“开始转换”可辨认，AI 外发说明在主动作下。**选中文件后仍以“拖入本地视频”为主体，文件名则在更换按钮之后作为弱提示，阅读顺序仍像未选文件的状态。**所选文件应成为该区域首要信息，拖入/更换作为后续动作。顶部为上一条合集任务的完成通知，已标出独立目标。 | 单帧不证明原始文件内容、系统选择器行为、拖入多文件或无效文件恢复。所选文件入口能辨认，不应把层级问题夸大为无法提交。 |
| 本地完成 · 54-local-start-progress | **虽然文件名带 progress，实际画面和 AX 均为完成：**“课程 A：风险与收益”、笔记已生成、阅读笔记。其结果层级与合集完成保持一致。 | 不能将 54 用作本地文件运行进度或暂停状态证据；主任务说明转换在截图前已完成。 |
| 离线入口 · 55-reader-offline-video | Reader 为 KEEP 课程，来源区提供“播放离线视频”；普通原视频入口仍可见，离线动作有明确名称。截图仍为按比例显示的合成素材。 | 入口存在不等于文件已经被正确打开；需结合 56。蓝色素材不足以评价真实课程图文。 |
| 实际播放器 · 56-offline-video-player | PNG 是打开的合成视频画面；AX 明确为 QuickTime Player，文件 URL 指向 `.course2md/work/task-18d38477b4701d80-e578-8/media.mp4`，与主任务提供的 KEEP task ID 相同。该证据能确认离线动作到达了该任务的本地媒体。 | QuickTime 标题显示“课程 A：风险与收益”，单凭标题差异不能判为打开错任务；具体文件路径指向 KEEP。单帧没有时间推进，不能证明播放持续、声音或整段媒体完整性。 |
| 存储正常 · 57-storage-before-relocate | 默认课程库的位置、打开位置和移动课程库动作同组；添加位置、存储详情为次级。设置保留“返回笔记”。该状态未见新的具体截断或主次问题。 | 此图只展示已登记位置，不证明文件实际可访问；失联状态由 58 覆盖。 |
| 位置失联 · 58-library-location-missing | 当前登记路径仍可见，标记“暂时不可访问”，说明笔记尚未读取且输入、任务仍保留；“重新定位”与原因相邻，未把失联误写为所有内容已删除。 | 这张图不证明保留的数据内容或选错文件夹时的行为。 |
| 重新定位完成 · 59-library-relocated | 库路径更新为 `relocated-library-wide`，后台卡的不可访问标记消失。成功结果给出找到的位置并说明文件留在所选处。**完成仍留在遮挡设置的对话层，需要“关闭”后才能返回笔记；重新定位已完成，没有剩余选择。**成功可在库卡附近反馈并解除阻断，失败仍保留明确恢复。 | 不能仅由成功文案证明文件未被改写；数据核对见下方。这里讨论的是成功后的多一步关闭，不涉及系统选择器排障。 |
| 恢复原笔记 · 60-reader-after-relocation | 返回 KEEP 同一课程及版本，离线入口、图片、图注均仍可见；与 55 相应内容位置一致，未丢到空库或另一篇笔记。 | 覆盖本次重新定位后的原笔记与当前可见位置；不证明任意长文位置、迁移后重新启动或再次离线播放。 |
| 搜索无结果 · 61-library-no-search-result | 保留搜索值、清除输入、筛选和视图控件；中心明确“没有匹配的笔记”，提供“清除搜索”。没有把筛选结果为空当作真正空库，也没有要求重新导入。该状态未见新的具体排版问题。 | 清除搜索后的列表恢复未在本组显示；不能推广到文件夹筛选为空或真正空库。 |

**运行和数据证据单独归属**

- 本代理额外读取了迁移后三份 manifest，标题分别为第 2 节、课程 A、KEEP，均为 `partial: false`、版本 1。该核对支持产物记录与画面目标相符，不替代正文质量判断。
- 主任务提供的任务记录确认：wide 运行新增三条完整任务；KEEP 的 task ID 为 `task-18d38477b4701d80-e578-8`，`keep_video=true`。56 的本地媒体 URL 与这个 ID 一致。
- 主任务另核对移动前 71 个文件：68 个逐字节不变，仅三份 `task-record.json` 更新关联路径，所有 artifact 均存在。本代理没有重复运行该校验；这是主任务的文件证据，不能改写为截图证明，也不能声称 71 个文件全都未变。

本次没有修改产品代码。50 红错误卡已有后续源码修改，另外三个层级/成功步骤发现已发回主任务；是否修正及新原生结果应追加实际证据。该组不覆盖窄窗、其他字号/配色、合集全部不可用、错误本地文件、离线媒体缺失或重新定位失败；这些是范围说明，不扩成新审计。
