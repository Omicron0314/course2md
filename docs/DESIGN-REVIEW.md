# course2md 设计评审记录（2026-09-14）

- 方法：4 路独立 subagent 评审，评审者先读 course2md-design 技能权威（SKILL.md + composition-review.md + system.md + layout-and-type.md + states-and-motion.md + interaction.md + settings.md），再逐张查看运行中原生应用（修复后构建，窗口 1122×806）截图 24 张；评审者未获得任何"已修复清单"，结论独立形成。
- 原始评审全文：/tmp/course2md-design/review-{workbench-tasks,library-reader,settings,crosspage}.md（本文件为合并裁决）。
- 证据缺口（评审者声明，转入最终验证）：hover/pressed/键盘焦点态、滚动条契约、窄/宽窗口、浅色主题的库/任务/服务/编辑器、任务展开态、服务列表滚动态。

## 高严重度（8）

| # | 区域 | 问题（证据） | 裁决/处置 |
| --- | --- | --- | --- |
| D1 | 跨页 | 内容列起始 x 四套并存：工作台/任务 ~132、笔记库 ~24、阅读正文 ~80、设置 ~286（crosspage#1） | 统一产品级内容标尺与阅读轴（工作台/库/任务/阅读同一边距体系；阅读标题/返回/工具条/正文同一外沿） |
| D2 | 工作台·高级选项 | 对象链打散+重复：「当前方式：GPU」与「识别方式 GPU」重复；识别方式/模型/模型状态三行平级散落，两端拉扯 1100px 空白（workbench#1/#6/#7/#8） | 按对象归组重排高级选项：三层用语（文字从哪来/在哪识别/用哪引擎）各自成组，设置单元 bound 到内容宽度，统一 text-start 轴 |
| D3 | 笔记库 | 文件夹三套控件并行：筛选轨、分组标题、卡内下拉；「按文件夹分组」两态语义倒置（开=弱文案「取消…」，关=选中胶囊）（library#1/#2/#5） | 文件夹单一对象：筛选轨保留；分组开关改为与「列表/卡片」同轨的固定标签两态；卡内归属控件收入溢出菜单 |
| D4 | 笔记库 | 卡片/列表结果动作不成立：卡片底栏全宽低强调「阅读笔记」，列表行尾「阅读」与「…」同级描边（library#3/#4） | 统一「打开已有笔记」共享角色（见 D7）；卡片=封面+元信息+主结果动作；「…」保持 quiet |
| D5 | 阅读页 | 「点击放大截图」tooltip 盖住摘要/正文/「从此处观看」（library#6、crosspage#6） | tooltip 锚定截图命中盒内且不得覆盖正文；放大即点击图片本身 |
| D6 | 服务编辑器 | 底部粘性动作栏裁切「模型 ID」输入行（crosspage#4）；「显示输入的密钥」无可见控件态（settings#11）；编辑器标题无图标（crosspage#4） | 表体与粘性栏之间留完整控件高度；密钥显示改为真 switch；标题走共享 heading 槽 |
| D7 | 跨页 | 「阅读笔记」结果动作角色分裂：工作台 outline、库列表 outline 短文案、库卡片全宽 outline、任务卡 filled（crosspage#3） | 指定共享角色：已有笔记的打开动作=outline+「阅读笔记」固定文案；仅任务完成卡当前主区域时 filled |
| D8 | 跨页 | 三层用语+模型名不统一：「文字来源」在两处不同义；「识别方式」在诊断行是引擎；模型名 Qwen3-ASR 1.7B vs Qwen3 1.7B（crosspage#2、workbench#13） | 全产品统一：文字从哪来（字幕/识别）/在哪识别（本机·服务）/用哪台引擎（GPU…）；诊断行改称「本机引擎」；模型显示名单一权威字符串 |

## 中严重度（16）

| # | 区域 | 问题 | 处置 |
| --- | --- | --- | --- |
| M1 | 共享原语 | 深色下「外观模式」分段轨道不可见；「文字来源」选中段跳出共享轨道成独立描边胶囊（settings#1、workbench#2） | SingleChoiceGroup 轨道/选中契约修复 + 逐主题对比校验 |
| M2 | 设置·生成笔记 | 「识别模型」选择器通栏宽、居中、无 chevron，读不出可点开（settings#2） | 加 chevron、左对齐、收敛宽度 |
| M3 | 设置·服务 | AI 服务卡裸域名无图标，与账号卡构成不一（settings#4） | 统一资源卡图标槽+层级 |
| M4 | 工作台 | 输入链接后无来源识别反馈（workbench#3） | 输入框旁即时平台识别反馈 |
| M5 | 工作台 | 空输入时「开始转换」与可用态无差别（workbench#4） | 空输入 disabled 态 |
| M6 | 任务页 | 「全部已结束」三级重复（页头/组头/badge）（workbench#5） | 保留卡内 badge；单状态少量时合并组头 |
| M7 | 阅读页 | 完成态首屏摘要后无章节标题直接大图，正文在折线下；行内截图按封面尺寸（library#7） | 摘要限高+章节标题入阅读轴；行内图限高 |
| M8 | 阅读页 | 目录轨常驻与工具行选中「目录」胶囊重复；轨内长标题折行掉字（library#8） | 宽窗轨常驻时工具行不占选中槽；标题列内换行、时间戳首行右栏 |
| M9 | 阅读页 | 截图卡片文字静默裁切无省略号（library#9） | 实测行盒或截断省略号 |
| M10 | 跨页 | 页标题全部无图标；分组标题有的有图标有的没有，标题槽位不一（crosspage#5、library#10、workbench#11） | 页标题与分组标题共用 heading 槽（图标槽+强调标签）；无图标也落 text-start |
| M11 | 跨页 | 深色主按钮与导航选中同为浅青，主操作不比导航更抢（crosspage#7） | 深色主操作提高与导航选中的分离 |
| M12 | 跨页 | 图标跨页指称混乱：tune 图标五处不同义；列表/菜单图标三处不同义（crosspage#8） | 每图标绑定一类对象 |
| M13 | 设置 | 「已下载」徽标用灰色信息样式而同类达成态用绿色对勾（settings#8） | 状态徽标语义统一 |
| M14 | 设置·存储 | 「刷新」悬在分区标题行尾远离作用对象（settings#6） | 刷新移入课程库卡片动作组 |
| M15 | 服务编辑器 | 「模型 ID」行尾无标签 ↻ 图标用途不明（settings#12）；取消孤立左下（settings#13） | icon-only 控件配标签/tooltip；取消并入动作组 |
| M16 | 工作台 | 高级选项 disclosure 两态样式不一致（quiet 行 vs 底色胶囊）（workbench#9） | 两态共用 quiet inline 样式 |

## 低严重度（合并处置）

设置：说明文字图标/位置规则不一（#3,#7,#10 并入共享 help/status 处理）；「语音服务」空分区无说明（#5 补低强调说明）；分区标题用 ⓘ 当类别图标（#9 换有意义图标）；模型状态与徽标分处两端（#10 合并状态说明）；「减少动态效果」三角形图标误读为 disclosure（#14 换动画语义图标）。工作台：「笔记内容」节名错位（#8 更名「文字来源」）；tab 与字段 label 重复且两 tab 起点不一（#10 去重保轴）；「最近笔记」无图标（#11 并入 M10）；品牌 logo 高饱和杂点+正文级字号（#12 降 metadata 行）；「模型/模型状态」同图标两义（#13 并入 D8）；「选择视频」图标不可识别（#14 换 folder_open 类）。跨页：刷新动作 icon-only vs 图标+标签不一（#9 并入 M14 统一）；任务计数接页标题行（#10 降 support 行）；服务卡动作截断省略号（#10 动作槽给满）。

## 二次评审要求（skill 强制）

修复完成后，对受影响页面重新截图，由未参与实现的独立 subagent 复审；实现者不得作为唯一终审。证据缺口清单一并补验。

---

# 第二轮独立复审记录（2026-09-14）

- 复审者：2 路独立 subagent（任务流 / 设置与跨页），同样先读技能权威再评当前构建截图，未获得修复清单。
- 总评：设置与跨页「基本达到可发布质量」；任务流「仍须返工」（阅读轴章节标题、高级选项对象行、库文件夹对象性、任务展开动作）。
- 复审全文：/tmp/course2md-design/review2-flow.md、review2-settings-cross.md。

## 第二轮发现的处置

| # | 发现 | 处置 |
| --- | --- | --- |
| flow#1 高 | 每章内每条时间戳标题都重复章节名；「从此处观看」挤在标题行 | 已修复：章节标题仅本章首条出现；观看动作贴到截图旁（56b1340） |
| flow#2 中 | 目录行长标题单字孤儿；工具行「目录」无选中态 | 已修复：单行省略+tooltip 全名；打开时 accent 文字色表达状态（56b1340） |
| flow#3 高 | 「中文·自动字幕」与「更换」两端撕开 | 已修复：值与动作收成一个对象行（56b1340） |
| flow#4 高 | 高级选项三层用语混排，自动字幕与自动选择像平行设置 | 已修复：解析结果改写为自动选择的说明（「将优先使用『中文 · 自动字幕』」）（56b1340） |
| flow#5 中 | 文件夹筛选无对象图标；卡片无归属；两组选择过近 | 已修复：真实文件夹带对象图标；卡片元信息含文件夹名（分组时除外）；平铺|分组与列表|卡片拉开间距（56b1340） |
| flow#6 中 | 「调整并生成新版」读作详情行 | 已修复：改 outline_pill 命令（61ef82d） |
| flow#7 低 | 「Bilibili 已识别 Bilibili 链接」三连 | 已修复：图标+一句「已识别 Bilibili 链接」（61ef82d） |
| flow#8 低 | 时长格式 0:07:37 vs 07:37 | 已修复：Source::detail 统一 fmt_ts（61ef82d） |
| settings#1 中 | 外观页两分段控件选中态似不一致 | 复核：同一 SingleChoiceGroup 实现与同一选中面；截图为不同捕捉时机/抗锯齿差异。定性为非问题（放大比对 crop2-mode/crop2-font 一致） |
| settings#2 中 | 应用页模型卡说明文字无共享信息图标 | 已修复：状态描述与网络范围说明走 supporting_info（d5eb005） |
| settings#3 低 | 「默认保存位置」徽标用 ⓘ | 已修复：Neutral 徽标改纯文字片，ⓘ 专属说明文字（d5eb005） |
| settings#4 低 | 「重新读取这些位置」读作静态文本 | 已修复：回到分区标题行尾的有标签「刷新」，与笔记库同模式（d5eb005） |
| settings#5 低 | 筛选 chip 描边选中 vs 分段/导航 tonal 选中并存 | 有意为之并记录：chip=描边选中（对象/集合过滤），分段/导航=tonal 选中。角色区分保留 |
| settings#6 低 | r2-07 与 fix-05 图标不一致 | fix-05 为修复前旧构建，非问题 |

## 首轮高严重度处置复核

D1-D8 全部修复并已在运行中验证（fix-01~05、r2-01~21 截图）：内容标尺统一、选择组轨道可见、高级选项对象归组、文件夹控件归一、「阅读笔记」共享角色、tooltip 不再遮挡、服务编辑器粘性栏不裁切、诊断行改「本机引擎」+ 模型名统一。M11（深色主按钮 vs 导航选中）复核为 filled accent vs tonal surface 分离成立，维持现状。目录轨内「目录」标题保留（标识轨道用途，偏差记录）。

## 遗留证据缺口（转最终验证）

hover/pressed/键盘焦点组合态、滚动条全状态、浅色主题库/任务/服务、全部调色板、100/125/150/200% 文字缩放、窄/宽窗口、服务编辑器测试/失败/保存流程、任务进行/失败态、空库与搜索无结果态、减少动态效果开态。

## 第三轮：运行验证（2026-09-15 凌晨）

在运行应用上做的端到端验证（非源码推断）：

- **模型下载全流程**：2.4GB 实下载，进度条持续推进（52MB→855MB）、速度与剩余时间实时更新、「暂停准备」生效、恢复时显示「尚未下载完整，继续准备会复用已有文件」。统一为 250ms 节拍与共享阶段判定后，卡片文案与进度一致。
- **任务调度全链路**：排队任务在无凭据需求时即时启动（run-task 子进程可见）→ 运行 → 完成。完成卡片为「阅读笔记」（填充强调）+「修复 AI 服务并补做」（描边恢复入口）+「查看任务详情」（安静动作）；AI 服务 401 被正确归类为凭据/权限问题，文案为「校对、摘要尚未完成。AI 服务未接受此任务的凭据或权限，请修复服务后补做。」；笔记库条目显示「部分内容待补全」徽记，任务终态为 partial。
- **引擎端到端（CLI，合成 62 秒中文语音视频）**：CoreML ASR 完整转写、笔记/HTML/ZIP 产物齐全；AI 服务不可达时保留原文并给出一次性明确警告。
- **冻结根因实证**：sample 显示主线程阻塞在 SecItemCopyMatching（后台 executor 任务经 GCD queue override 跑在主线程）；全部阻塞 IO 迁至 `spawn_blocking_io`（专用 OS 线程）后，主线程回到 run loop 空闲等待，Keychain 弹窗排队期间界面保持可用。
- **浅色 Paper 主题 / 降低动态效果开关 / 高级选项对象行 / 阅读器修复 / 笔记库文件夹图标与间距**：截图逐项确认（r6 系列）。

环境备注：验证期间另一应用的 Keychain 密码弹窗（SecurityAgent）会抑制合成输入，属系统行为；course2md 自身在该场景下已不再冻结。
