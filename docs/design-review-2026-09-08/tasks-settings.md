# 任务、设置与管理页面设计审查

审查日期：2026-09-08。范围为 `task_ui.rs`、`settings_ui.rs`、`model_diagnostics.rs`、`account_ui.rs`、`storage_ui.rs` 与 `about.rs` 的实际页面、子区块和弹窗。依据用户标注截图、当前实现、`UX-VALIDATION-2026-09-07.md`、`DESKTOP-DESIGN-SYSTEM.md` 和 `DESKTOP-SETTINGS-REVIEW.md`；本轮新项目 skill 建立后作为最终规范。

本记录的首轮结论来自源码审查，不把历史截图或编译通过当作本轮视觉验收。用户已取消可访问性专项，审查集中在正常使用中的层级、操作可发现性、配色、图标、文案和反馈。

## 已逐页检查的范围

| 页面 | 状态与子区块 |
| --- | --- |
| 任务总览 | 空列表、不可读取、历史任务材料、普通任务卡、选中任务 |
| 任务详情 | 排队、运行、暂停中、暂停、失败、部分完成、完成、后续补做、未知请求与明确重发、技术详情 |
| 工作台任务卡 | 运行进度、并行阶段、已完成步骤、等待队列、暂停与取消、需要处理、部分完成恢复 |
| 设置 → 生成笔记 | 默认作用范围、字幕语言、识别方式、识别模型、模型缓存/下载/加载、AI 校对与摘要、截图开关、自定义规则、导出格式与视频保留 |
| 设置 → 服务与账号 | 已保存服务、停用服务、服务草稿、添加/编辑卡、当前任务服务弹窗、协议/地址/认证/密钥、主动测试三种 AI 用途、测试结果、发布/绑定失败、停止服务确认 |
| Bilibili 账号 | 未登录、已登录、失效、验证中、检查失败、退出失败；二维码获取、等待扫码、手机确认、过期、网络错误、成功、返回来源重试 |
| 设置 → 存储 | 默认和附加位置、离线位置、打开/添加/设为默认、迁移、关联修复、未完成迁移继续/结束、旧位置备份与清理 |
| 存储操作弹窗 | 读取关联、复制/核验迁移、取消中、失败、完成、备份清理确认 |
| 设置 → 应用 | 外观、文字大小、动效选项、关于、旧设置发现/导入/损坏修复/备份、运行诊断、模型与工具技术详情 |
| 关于 | 产品版本与构建、项目/反馈/许可链接 |

## 首轮发现与处置要求

| 编号 | 严重度 | 位置 | 当前问题 | 修正要求 |
| --- | --- | --- | --- | --- |
| TS01 | 高 | `task_ui.rs:1120` | 总览只有标题样式的 ghost 按钮和灰色状态；缺少页内任务摘要，点击区域与查看详情动作不清楚。 | 明确任务标题、计数/状态摘要；整齐的行头、状态图标徽标及详情展开指示。 |
| TS02 | 高 | `task_ui.rs:1462` | 进度条直接跳到新宽度；排队/启动/进行中缺少运动线索，已完成步骤用文本 `✓`。 | 按真实进度平滑插值；未知总量用持续但轻量的忙碌指示；完成步骤统一 Material 状态图标。 |
| TS03 | 中 | `task_ui.rs:1605` | 暂停中仍保留“暂停生成”可点击，无法从操作区判断请求已被接受。 | 暂停中改为有忙碌提示的“正在暂停”，禁止重复操作；取消采用清楚的次要动作。 |
| TS04 | 高 | `settings_ui.rs:619` | 生成默认页堆叠字幕优先策略、语言代码、硬件简称、GGUF、段落 ID/返回结构等技术文案。 | 默认呈现自然语言选择和简短用途；自定义语言代码、固定硬件、模型缓存清单与协议规则按需展开。保留影响费用和数据发送的说明。 |
| TS05 | 中 | `settings_ui.rs:503` | 四页签纯文本，设置组标题/添加/编辑/测试/恢复动作缺少一致图标。 | 使用统一 Material 图标；分组、主次动作、表单图标尺寸与间距遵循项目 token。 |
| TS06 | 高 | `settings_ui.rs:1735` | 服务测试通过/失败/不确定结果都是普通小字；保存状态和测试状态靠连续段落区分。 | 用真实结果映射的状态卡与图标，突出可恢复动作；保留“测试通过后需要保存”的明确状态，禁止误报成功。 |
| TS07 | 中 | `settings_ui.rs:973` | 服务元信息把主机、协议、模型和版本连接成一长行；服务卡停止动作与常用动作没有明显主次层级。 | 服务名/真实状态一行，模型/主机为次级信息；编辑/测试为常用动作，停止服务保持次要且有确认。 |
| TS08 | 高 | `settings_ui.rs:2820` | 应用诊断默认展示工具清单；可选 `llama-server` 缺失与核心媒体能力并排，正常 Apple 后端也容易被理解为应用未就绪。 | 首层展示“视频处理、在线读取、本机识别”能力；工具名、缓存路径、安装命令收进诊断详情，只在对应能力确实缺失时给修复动作。 |
| TS09 | 中 | `account_ui.rs:347` | 二维码获取时显示固定白色 260px 面板与静态 LoaderCircle；扫码/确认/过期变化靠文字替换。 | QR 保持白色扫描背景；生成/确认/成功/失败使用主题状态区域、忙碌动画与短状态转场，使用真实品牌标识。 |
| TS10 | 中 | `storage_ui.rs:786` | 存储弹窗只有进度文字和控件，完成/失败缺少图标；取消请求后的按钮仍像初始动作。 | 用操作状态图标与轻量进度反馈，取消处理中显示已接受的状态；完成后明确关闭。 |
| TS11 | 中 | `settings_ui.rs:2440` | 每个存储卡都常驻“设为默认保存位置”，当前默认卡显示禁用按钮；长路径与多个同级按钮挤占视觉权重。 | 默认徽标替代无效按钮；图标化打开/移动动作，路径为次级信息，危险或低频操作降低权重。 |
| TS12 | 中 | `task_ui.rs`、`settings_ui.rs`、`model_diagnostics.rs`、`storage_ui.rs` | 错误/警告颜色直接使用 `0xa32626`/`0x945000`，状态和表面不能可靠跟随深浅主题。 | 所有产品颜色通过语义 token；QR 的黑白为扫描必要例外。 |
| TS13 | 中 | `settings_ui.rs:2638` | 应用外观只有字号和减少动态效果，没有可持久化配色主题配置。 | 增加统一主题选择入口，支持跟随系统及明暗配色，选择后即时预览并保存。此项由主代理实现 `appearance_ui.rs`。 |
| TS14 | 低 | `about.rs:38` | 关于链接是无图标的右对齐文字按钮；“帮助”实际指向项目仓库。 | 加外链/反馈/许可 Material 图标，并把目的地标为“项目主页”。 |

## 验证记录

- 首轮已完成上述页面源码与既有验收记录对照。
- 实现依据为项目内 [course2md-design skill](../../.agents/skills/course2md-design/SKILL.md)。
- 修正后所属六个 Rust 文件经过 `rustfmt --edition 2024`，`git diff --check` 通过。
- 全项目整合检查由主代理执行，原生截图复核与交互观察由主代理组织后补充；没有把源码修正等同于原生视觉通过。

## 实现闭环

| 编号 | 已落地修正 | 验证状态 |
| --- | --- | --- |
| TS01 | 任务页增加标题和真实计数；任务头使用包含 Material 图标、两行标题、创建时间、状态徽标和展开箭头的完整 Button，能展开/收起，使用标准悬浮与按下反馈。 | 源码完成；待原生复核 |
| TS02 | 任务详情和工作台均使用共享 `motion::progress`；启动、进行阶段、模型准备、缓存检查均有真实 busy spinner。完成步骤替换为 Material check_circle。动画 ID 绑定任务/阶段。 | 源码完成；待运行进度复核 |
| TS03 | 暂停中改为“正在暂停…”，busy/禁用状态阻止重复操作，取消保留独立的次要入口。 | 源码完成；待原生复核 |
| TS04 | 字幕语言提供自动及常用语言，自定义代码置于展开区域；识别首层为自动、本机、在线服务，固定硬件收进高级设置；模型管理、缓存清单、AI 选项及校对规则用共享 disclosure。保留语言草稿、费用与外发说明。 | 源码完成；待原生复核 |
| TS05 | 设置增加外观入口，顺序为外观、生成笔记、服务与账号、存储、应用。5 个页签和各分组、重要操作统一 Material 图标。原偏好索引和保存域保留。 | 源码完成；待原生复核 |
| TS06 | 服务测试结果使用真实 outcome 映射的成功/未知/未发送/失败徽标与语义背景，测试有 spinner，详细证据可展开，保存后的使用规则仍明确；保存失败保留原重试动作并增加状态进入反馈。 | 源码完成；待故障服务复核 |
| TS07 | 服务元信息压缩为模型与主机；添加、编辑、测试、停用统一图标。任一现有测试未通过时不会继续显示泛化的成功徽标。停用确认与实际后续请求边界保留。 | 源码完成；待原生复核 |
| TS08 | 运行检查首层改为视频处理与导出、读取在线视频、本机语音识别三项实际能力；可选运行时和硬件清单收进诊断详情。真正缺少必需组件时保留下载/复制安装命令和重新检查动作。 | 源码完成；待原生复核 |
| TS09 | 账号卡和二维码页使用真实 Bilibili 标识；生成 QR、验证登录、等待手机确认有 spinner；获取/等待/成功/失败分状态进入，失败和成功使用主题表面。QR 黑白扫描区明确作为主题例外。 | 源码完成；待隔离账号状态复核 |
| TS10 | 存储操作加入忙碌/成功/失败图标和真实比例的平滑进度；取消处理中改为“正在结束…”并禁止重复点击；关联、迁移、备份打开与清理操作增加一致图标。 | 源码完成；待隔离迁移复核 |
| TS11 | 当前默认存储位置只保留徽标，其他位置才提供“设为默认”；打开/移动用明确图标动作，旧备份清理降为 quiet 操作并保留确认。 | 源码完成；待原生复核 |
| TS12 | 六个 UI 文件中的产品颜色全部改为 `color(语义 token)`；错误/警告移除固定十六进制颜色，状态文本 glyph 已替换。剩余黑白值只用于真实 QR 扫描。 | 静态覆盖检查通过；待明暗主题复核 |
| TS13 | 外观独立页由主代理提供；本模块公开原有字号/动效控件及保存接口，外观页接收应用偏好保存失败反馈与重试入口。 | 接口整合完成；主题持久化由主代理验证 |
| TS14 | 关于的反馈、项目与许可入口补 Material 图标；“帮助”更名为与实际目的地一致的“项目主页”。 | 源码完成；待原生复核 |

本轮没有改变服务测试的主动授权、未确认请求的明确重发、不可逆清理确认、既有笔记保存和迁移校验语义；尚未访问的网络/异常状态仍标记待运行验证。

## 原生截图复审

### 深色外观页（2026-09-08，04-appearance-dark）

查看主代理提供的实际 GPUI 窗口截图 `evidence/04-appearance-dark.jpg`：

- 设置 5 个页签的顺序、图标、选中项及内容起始线一致，没有溢出或额外白色表面。
- 深色主题卡按 3 列对齐，主题名称与缩略画面可辨；当前 Ink 同时有强调边框和勾选标记。
- 页面下缘是滚动视口对下一行卡片的正常裁切，不能据此认定控件被遮挡；本图未覆盖下方字号/动效组。
- 发现外观模式选择的选中背景与父卡同为 `SURFACE`，“跟随系统”主要靠文字变亮体现，识别较弱。已反馈主代理调整 `appearance_ui.rs` / 共享模式选择样式，等待复核。
- 顶部“新笔记”小浮层由主代理继续核验 tooltip 生命周期，暂不作为外观页自身缺陷。

本图只核验稳定画面，不宣称已经测得动效逐帧表现或系统明暗自动切换。

### 浅色生成、服务列表与行内编辑（05 / 06 / 07）

- `05-settings-generation-light.jpg`：文字来源与语音识别分组、输入/选择器边界和选中项清楚；源码保留的高级设置未展开，本图不代表其内部内容已验证。发现模型结论再次显示内部 `qwen3-1.7b`，而上方选择器已展示正式名称；已改为“模型尚未准备，开始识别时会自动下载”等直接结论，等待后续构建复核。
- `06-settings-services-light.jpg`：空服务分组、草稿继续填写、账号状态和登录操作分区清楚；真实 Bilibili 图形可辨但默认呈灰色，已请主代理将图标 helper 设置为固定品牌蓝。
- `07-service-editor-light.jpg`：实际为行内编辑卡。服务名称、地址、模型 ID 与认证分组对齐，输入边界和当前焦点可见。接口 radio 暴露完整路由使标签过长，已将可见标签改为“语音转录 / 音频对话 / AI 对话”；内部协议标识保持不变，地址下保留真正请求 endpoint。
- 依据跨页审查，删除了设置返回按钮旁常驻的“当前草稿与填写位置已保留”；返回行为和草稿恢复逻辑保持原样。

上述截图还没有覆盖编辑器底部的测试结果、保存/绑定错误及密钥状态，不能将这些场景记为视觉通过。

### 服务测试、存储、应用与任务（08–14）

- `08-service-testing-light.jpg`：真实 busy 图标、禁用测试按钮和保存/取消入口可见。发现“正在测试服务”与具体测试步骤重复；已合并为一条带 spinner 的实际步骤。
- `09-service-result-light.jpg`：通过结果位于绿色状态卡，保存仍为明确主操作，测试和取消为次操作。将重复的通过文案合并成包含实际用途的徽标，仍保留“保存服务后使用这些更改”和可展开证据。
- `10-settings-storage-light.jpg` / `11-storage-details-light.jpg`：默认位置只显示徽标，移动、打开、添加的动作可识别，库 ID 仅在明确展开后出现。去掉了与位置选择无关的常驻缓存管理解释。此组截图不覆盖迁移中和恢复弹窗。
- `12-settings-application-light.jpg` / `13-diagnostics-light.jpg`：关于版本/构建、项目/反馈/许可入口对齐；运行检查按能力展示真实状态；工具清单在展开后出现，未见无关可选运行时的主界面告警。
- `14-tasks-light.jpg`：任务头的图标、标题、时间、状态和展开箭头均可辨，阅读为明确主动作。发现外层与内部重复“任务”标题，已反馈主代理移除外层重复标题；完整的已完成阶段列表占用过多高度，已移入技术详情。运行、失败和暂停任务仍保留实际进度与恢复入口。

本批截图覆盖的是浅色下稳定的测试/成功、存储正常/详情、关于、诊断与任务完成态。所述新修正需要用后续构建复查；失败服务、二维码和迁移中的实际画面仍单独待验证。

### 十种主题的外观页静态复核

已逐一查看 `04-appearance-dark.jpg`（Ink）和 `theme-*.png` 中的 Paper、Nord、Nord Snow、Tokyo Night、Tokyo Day、Catppuccin Latte / Frappé / Macchiato / Mocha。每套主题的设置壳、正文/次级文字、卡片表面、强调色及当前选择边框/勾选均跟随当前主题，未发现新的混色或固定白色表面。

外观模式选中背景较弱是这些首轮截图的共同已知问题，主代理已安排修正；截图不用于宣称重启持久化、系统自动切换或逐帧动画已验证。

### 真实运行任务、完成通知与二维码（23 / 24 / 29）

- `23-task-running-light.jpg` 实际显示“下载视频 · 已用 1 秒”：运行状态、busy 图标、已完成的字幕步骤、暂停与取消入口清楚，未知总量未冒充百分比。该图提供真实运行状态画面，旋转/插值的实现依据仍为共享 `motion.rs`，单帧不用于量化流畅度。
- `24-queue-running-light.jpg` 虽然文件名含 `running`，画面已是“笔记已生成”，列表计数也为 0 个未完成。此图仅记录完成通知、查看生成结果和阅读入口；重复标题/已完成步骤属于修正前构建，等待后续复核。
- 观察到 24 中新任务详情可见时标题栏仍有 `任务 · 1`，已反馈主代理核实通知/未读数的消费时机，未擅自修改通知策略。
- `29-account-panel-light.png`：登录弹窗的 QR 区域、品牌标识、扫码说明、真实过期时间和取消按钮布局完整，白色扫描区域与外围留白清楚。主代理只打开并随后取消临时二维码，没有扫码登录或保存真实账号；不得将此图写成登录成功证据。

23 与 29 的已见稳定画面没有新增排版缺陷。本轮仍未观察二维码过期/错误/成功、迁移取消/恢复的实际最终界面。

## 最终修订复核（design05 / design06）

主代理提供并确认窗口实际为 **860×620**。独立查看最终原生截图后：

| 证据 | 观察与结论 |
| --- | --- |
| `37-appearance-narrow-dark.jpg` | 设置页保留单一返回按钮，已删除草稿保存的常驻说明。5 个图标页签完整；主题区使用双列，没有横向溢出。当前“跟随系统”有明确强调底色与边框；文字显示浅色 Catppuccin Latte、深色 Tokyo Night 的独立选择。原模式选中偏弱问题关闭。 |
| `38-appearance-narrow-light.jpg` | 相同窄窗切到浅色后，“浅色”模式的强调底色、边框与文字均清楚；Catppuccin Latte 的表面、文字、导航和卡片保持一致，双列结构完整。主题卡超出首屏的部分处于正常滚动区域，不是覆盖或横向裁切。 |
| `45-tasks-narrow-dark.jpg` | 页面只有一个“任务”标题和真实计数。已选的完成任务只呈现结果、保存位置、调整和阅读入口，完整步骤收进技术详情。其他任务保持紧凑行卡；阅读按钮和状态徽标清楚。进入可见任务后标题栏未再残留“未读 1”。重复标题、完成过程过度展开和已读消费问题关闭。 |

上述三张最终截图中没有发现新的具体设计缺陷。主代理另报告全桌面 **115 项测试通过**；本子代理没有重复运行测试，也不把这项结果当作对所有未访问状态的视觉证明。

### 当前覆盖结论

- 任务总览/完成详情、设置壳、外观、十种主题预览、生成基础选项、服务草稿与行内编辑、真实服务测试/成功、账号 QR、正常存储/详情、关于和运行诊断均有本轮原生截图复审。
- 最终修订已实机复核窄窗明暗外观和深色任务完成态，前文“待后续构建”的模式选择、返回说明、重复任务标题和完成步骤问题已经关闭。
- 暂停/取消处理中、未知或失败服务、二维码过期/成功、迁移/恢复/备份清理异常等仍以源码与既有行为校验覆盖；本轮没有逐个访问其最终原生状态，不记为视觉通过。没有把历史流程验收或截图文件名当作本轮证据。
- 主代理负责主题重启持久化、独立明暗偏好及总体动效运行测试。本记录只陈述独立审查所见，没有新增遗留的明确设计缺陷。
